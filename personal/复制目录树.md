## 记录目录树（不包含文件）

```
find . -type d > dirs.txt
```

如果要包含文件，改用tree
```
tree . > dirs.txt
```


## 创建空目录树

```
mkdir -p $(cat dirs.txt)      
```

如果路径包含空格，改用 xargs

```
xargs -I {} mkdir -p "{}" < dirs.txt   
```
